.DS_Store
node_modules

# Build output
/build
/.svelte-kit
/package
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Env
.env
.env.*
!.env.example

# Generated by `node compat/make.mjs`. The generator is tracked, the fixtures
# are not: a file of raw ESC bytes is unreadable in a diff, and the flood set is
# 300 MB.
compat/ansi/
compat/unicode/
compat/osc/
compat/flood/

# Regenerated by `npm run tauri icon`. Only the desktop sizes referenced in
# tauri.conf.json are tracked; the mobile and Windows Store variants are not.
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/Square*.png
src-tauri/icons/StoreLogo.png
